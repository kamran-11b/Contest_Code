#include<stdio.h>
#include<string.h>

/*
void reverse_string(char *ch)
{
    for(int i=0; i<strlen(ch)/2; i++)
    {
        char c=ch[i];
        ch[i]=ch[strlen(ch)-i-1];
        ch[strlen(ch)-i-1]=c;
    }
}

int main()
{
    char ch[50];
    scanf("%[^\n]s",&ch);
    reverse_string(ch);
    printf("%s\n",ch);
}
*/

